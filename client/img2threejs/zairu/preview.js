import * as THREE from '/vendor/three.module.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { createZairuPlayableHeroModel, createZairuPlayableHeroLookDevLights } from './createZairuModel.js';
const scene=new THREE.Scene();scene.background=new THREE.Color(0x10161c);const camera=new THREE.PerspectiveCamera(32,innerWidth/innerHeight,.05,100);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setSize(innerWidth,innerHeight);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.shadowMap.enabled=true;document.body.append(renderer.domElement);const model=createZairuPlayableHeroModel();scene.add(model,createZairuPlayableHeroLookDevLights());const ground=new THREE.Mesh(new THREE.CircleGeometry(4,48),new THREE.MeshStandardMaterial({color:0x1b272c,roughness:.9}));ground.rotation.x=-Math.PI/2;scene.add(ground);const c=new THREE.Vector3(0,1.7,0);camera.position.set(0,1.7,7);camera.lookAt(c);const controls=new OrbitControls(camera,renderer.domElement);controls.target.copy(c);controls.update();window.reviewModel=model;document.documentElement.dataset.reviewContract='valid';document.documentElement.dataset.reviewHeroId='zairu';document.documentElement.dataset.reviewTriangles=String(model.userData.characterModel.performance.triangles);document.documentElement.dataset.reviewDrawCalls=String(model.userData.characterModel.performance.drawCalls);renderer.setAnimationLoop(()=>{controls.update();renderer.render(scene,camera)});addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});
