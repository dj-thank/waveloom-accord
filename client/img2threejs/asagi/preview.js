import * as THREE from '/vendor/three.module.js'; import {createAsagiPlayableHeroModel,createAsagiPlayableHeroLookDevLights} from './createAsagiModel.js'; const s=new THREE.Scene();s.background=new THREE.Color(0x10151b);const c=new THREE.PerspectiveCamera(32,innerWidth/innerHeight,.05,100);c.position.set(0,2.1,7);const r=new THREE.WebGLRenderer({antialias:true});r.setSize(innerWidth,innerHeight);r.outputColorSpace=THREE.SRGBColorSpace;document.body.append(r.domElement);const m=createAsagiPlayableHeroModel();s.add(m,createAsagiPlayableHeroLookDevLights());document.getElementById('status').textContent=`asagi ${m.userData.characterModel.performance.triangles} tris`;r.setAnimationLoop(()=>r.render(s,c));
