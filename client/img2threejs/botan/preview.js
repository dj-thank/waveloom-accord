import * as THREE from '/vendor/three.module.js';import{OrbitControls}from'three/addons/controls/OrbitControls.js';import{createBotanPlayableHeroModel,createBotanPlayableHeroLookDevLights}from'./createBotanModel.js';const scene=new THREE.Scene();scene.background=new THREE.Color('#111820');const camera=new THREE.PerspectiveCamera(32,innerWidth/innerHeight,.05,100);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setSize(innerWidth,innerHeight);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.shadowMap.enabled=true;document.body.append(renderer.domElement);const model=createBotanPlayableHeroModel();scene.add(model,createBotanPlayableHeroLookDevLights());const box=new THREE.Box3().setFromObject(model);model.position.y-=box.min.y;const center=box.getCenter(new THREE.Vector3());camera.position.set(0,1.7,7);camera.lookAt(center);const ground=new THREE.Mesh(new THREE.CircleGeometry(4,48),new THREE.MeshStandardMaterial({color:'#1b242b',roughness:.9}));ground.rotation.x=-Math.PI/2;ground.position.y=-.02;ground.receiveShadow=true;scene.add(ground);const controls=new OrbitControls(camera,renderer.domElement);controls.target.copy(center);controls.enableDamping=true;document.getElementById('status').textContent=`ready · ${model.userData.heroId} · ${model.userData.characterModel.performance.triangles.toFixed(0)} tris`;document.documentElement.dataset.reviewReady='true';document.documentElement.dataset.reviewContract='valid';renderer.setAnimationLoop(()=>{controls.update();renderer.render(scene,camera)});addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});
