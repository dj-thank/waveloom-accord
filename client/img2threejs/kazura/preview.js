import * as THREE from '/vendor/three.module.js';
import {OrbitControls} from 'three/addons/controls/OrbitControls.js';
import {createKazuraPlayableHeroModel,createKazuraPlayableHeroLookDevLights} from './createKazuraModel.js';
const scene=new THREE.Scene(); scene.background=new THREE.Color(0x101810); const camera=new THREE.PerspectiveCamera(32,innerWidth/innerHeight,.05,100); const renderer=new THREE.WebGLRenderer({antialias:true}); renderer.setSize(innerWidth,innerHeight); renderer.outputColorSpace=THREE.SRGBColorSpace; renderer.shadowMap.enabled=true; document.body.append(renderer.domElement); const model=createKazuraPlayableHeroModel({castShadow:true,receiveShadow:true}); scene.add(model,createKazuraPlayableHeroLookDevLights()); const ground=new THREE.Mesh(new THREE.CircleGeometry(4,48),new THREE.MeshStandardMaterial({color:0x213127,roughness:.9})); ground.rotation.x=-Math.PI/2; ground.receiveShadow=true; scene.add(ground); const controls=new OrbitControls(camera,renderer.domElement); controls.target.set(0,1.25,0); camera.position.set(3.3,2.3,6.5); controls.update(); document.getElementById('status').textContent=`Kazura ready | ${model.userData.characterModel.performance.triangles.toFixed(0)} tris | contract valid`; window.reviewModel=model; renderer.setAnimationLoop(()=>{controls.update(); renderer.render(scene,camera);}); addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight);});
