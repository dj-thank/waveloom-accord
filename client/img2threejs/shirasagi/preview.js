import * as THREE from '/vendor/three.module.js';import {createShirasagiPlayableHeroModel} from './createShirasagiModel.js';
const scene=new THREE.Scene();scene.background=new THREE.Color(0x10151d);const camera=new THREE.PerspectiveCamera(32,innerWidth/innerHeight,.1,100);camera.position.set(4,2.8,6);camera.lookAt(0,1.8,0);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;document.body.append(renderer.domElement);scene.add(new THREE.HemisphereLight(0xdcecff,0x202938,1.6));const key=new THREE.DirectionalLight(0xffe5bf,3);key.position.set(-3,6,5);scene.add(key);const hero=createShirasagiPlayableHeroModel();scene.add(hero);const floor=new THREE.Mesh(new THREE.PlaneGeometry(12,12),new THREE.MeshStandardMaterial({color:0x20252b}));floor.rotation.x=-Math.PI/2;scene.add(floor);function loop(t){hero.rotation.y=t*.00025;renderer.render(scene,camera);requestAnimationFrame(loop)}loop(0);
