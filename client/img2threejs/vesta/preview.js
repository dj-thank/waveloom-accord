import * as THREE from '/vendor/three.module.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { createVestaPlayableHeroModel, createVestaPlayableHeroLookDevLights } from './createVestaModel.js';
const scene = new THREE.Scene(); scene.background = new THREE.Color('#121820'); const camera = new THREE.PerspectiveCamera(32, innerWidth / innerHeight, .05, 100); const renderer = new THREE.WebGLRenderer({ antialias: true }); renderer.setSize(innerWidth, innerHeight); renderer.outputColorSpace = THREE.SRGBColorSpace; renderer.shadowMap.enabled = true; document.body.append(renderer.domElement); const model = createVestaPlayableHeroModel(); scene.add(model, createVestaPlayableHeroLookDevLights()); const ground = new THREE.Mesh(new THREE.CircleGeometry(4, 48), new THREE.MeshStandardMaterial({ color: 0x1f2a2d, roughness: .9 })); ground.rotation.x = -Math.PI / 2; ground.position.y = -.02; ground.receiveShadow = true; scene.add(ground); const controls = new OrbitControls(camera, renderer.domElement); camera.position.set(0, 2.1, 7); controls.target.set(0, 2, 0); controls.update(); window.reviewModel = model; document.documentElement.dataset.reviewReady = 'true'; document.documentElement.dataset.reviewHeroId = model.userData.heroId; document.documentElement.dataset.reviewContract = 'valid'; addEventListener('resize', () => { camera.aspect = innerWidth / innerHeight; camera.updateProjectionMatrix(); renderer.setSize(innerWidth, innerHeight); }); renderer.setAnimationLoop(() => { controls.update(); renderer.render(scene, camera); });
